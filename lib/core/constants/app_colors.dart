import 'package:flutter/material.dart';

class AppColors {
  //primary app colors
  static const Color primary = Color(0xFF1a2a4a); // Fixed alpha to FF
  static const Color secondary = Color(0xFF2ed573); // Fixed alpha to FF
  static const Color background = Color(0xFFF8F9FA); // Fixed alpha to FF
  static const Color text = Color.fromARGB(255, 52, 58, 64); // Fixed alpha to 255
  static const Color icons = Color(0xFF343A40); // Fixed alpha to FF

  // Secondary app colors
  static const Color secondaryButton = Color.fromARGB(255, 35, 56, 98);
  static const Color highlights = Color.fromARGB(255, 255, 180, 0); // Fixed alpha to 255
  static const Color warnings = Color(0xFFFF6B6B); // Fixed alpha to FF
  static const Color secondaryText = Color(0xFFAAB0BC); // Fixed alpha to FF
  static const Color borders = Color(0xFFE0E0E0); // Fixed alpha to FF

}